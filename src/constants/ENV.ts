/**
 * Because the limitation of time,
 * So we focused on features more.
 * Still use hard code for the environment here
 * In the future, we can use .env file to manage the environment
 * and use this file to export the environment variables
 * to save changes in the future
 */

export const ENV = {
  API_URL: 'https://binaural-beats-audio-services.onrender.com/api',
};
